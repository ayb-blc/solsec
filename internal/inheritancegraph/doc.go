// Package inheritancegraph builds project-level Solidity context for detectors.
//
// It models contracts, inheritance edges, functions, modifiers, signatures, and
// ordered state operations so detectors can reason across files instead of
// relying only on line-oriented source scans.
package inheritancegraph
